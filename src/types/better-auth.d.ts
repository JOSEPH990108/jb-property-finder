// src\types\better-auth.d.ts
declare module "better-auth" {
  // You can type the exports properly if needed, but this is enough to suppress the error:
  export function betterAuth(config: any): any;
}